/* eslint-disable @typescript-eslint/no-explicit-any */
import { money } from "@/lib/pricing";

export default function OnSiteAddOnsPanel({ onSiteAddOns, selectedOnSiteAddOns, toggleOnSiteAddOn, selectedOnSiteAddOnsTotal, paidOnSiteAddOns, payOnSiteAddOns, joined, waitlisted }: any) {
  return <div className="mt-5 rounded-2xl border border-stone-200 bg-white p-5 shadow-sm"><div className="flex flex-col gap-3 md:flex-row md:items-start md:justify-between"><div><div className="text-xs font-black uppercase tracking-wide text-stone-500">Допы во время события</div><h3 className="mt-1 text-xl font-black">Можно докупить позже</h3><p className="mt-1 text-sm leading-6 text-stone-600">Например, халат, тапочки, веник или дополнительный час. Это допы владельца места, которые участник может добавить после базовой оплаты или на месте.</p></div><div className="rounded-2xl bg-stone-50 px-4 py-3 text-sm font-black text-stone-800">Допы: {money.format(selectedOnSiteAddOnsTotal)} ₽</div></div><div className="mt-4 grid gap-2 sm:grid-cols-2">{onSiteAddOns.map((addon: any) => <label key={addon.id} className="flex cursor-pointer items-center justify-between rounded-2xl bg-stone-50 px-4 py-3 hover:bg-stone-100"><span className="flex items-center gap-3"><input type="checkbox" checked={selectedOnSiteAddOns.includes(addon.id)} onChange={() => toggleOnSiteAddOn(addon.id)} className="h-4 w-4 accent-stone-950" /><span className="text-sm font-bold">{addon.title}</span></span><b className="text-sm">+{money.format(addon.price)} ₽</b></label>)}</div><button disabled={!joined || waitlisted || !selectedOnSiteAddOns.length} onClick={payOnSiteAddOns} className={`mt-4 w-full rounded-2xl px-5 py-4 font-black text-white ${paidOnSiteAddOns ? "bg-emerald-600" : joined && !waitlisted && selectedOnSiteAddOns.length ? "bg-stone-950 hover:bg-stone-800" : "bg-stone-400"}`}>{paidOnSiteAddOns ? "Допы оплачены" : selectedOnSiteAddOns.length ? "Оплатить выбранные допы" : "Выберите допы"}</button>{(!joined || waitlisted) && <p className="mt-2 text-xs font-bold text-stone-500">Оплатить допы можно после присоединения к событию.</p>}</div>;
}
