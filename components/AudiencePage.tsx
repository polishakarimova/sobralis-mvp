/* eslint-disable @typescript-eslint/no-explicit-any */
import Field from "./Field";

export default function AudiencePage({ title, subtitle, bullets, button, leadSent, setLeadSent }: any) {
  return <main className="mx-auto max-w-6xl px-4 py-12 sm:px-6 lg:px-8"><div className="grid gap-8 lg:grid-cols-[1fr_0.9fr]"><div className="rounded-[2rem] border border-stone-200 bg-white p-8 shadow-sm"><div className="mb-4 inline-flex rounded-full bg-amber-50 px-3 py-1 text-xs font-black text-amber-800">Собрались · Калининград</div><h1 className="text-5xl font-black tracking-tight">{title}</h1><p className="mt-5 max-w-2xl text-xl leading-8 text-stone-600">{subtitle}</p>{title === "Для владельцев мест" && <div className="mt-6 rounded-[1.5rem] border border-emerald-100 bg-emerald-50 p-5"><div className="text-sm font-black uppercase tracking-wide text-emerald-800">Новая ценность для объекта</div><h2 className="mt-2 text-2xl font-black">Одна бронь приводит не одного клиента, а всю компанию</h2><p className="mt-2 text-sm leading-6 text-stone-700">Обычно баня знает только организатора. В “Собрались” каждый участник может присоединиться к событию, оплатить своё место и остаться в базе сервиса как гость этого объекта. Дальше объект сможет возвращать гостей через предложения внутри платформы, не собирая телефоны вручную.</p></div>}<div className="mt-8 grid gap-3">{bullets.map((item: string) => <div key={item} className="rounded-2xl bg-stone-50 p-4 font-bold">✓ {item}</div>)}</div></div><div className="rounded-[2rem] border border-stone-200 bg-white p-8 shadow-sm"><h2 className="text-2xl font-black">Оставить заявку</h2><p className="mt-2 text-stone-600">Это демо-форма. В первой версии заявки можно отправлять в Telegram/таблицу.</p><div className="mt-6 grid gap-4"><Field label="Имя / название"><input className="input" placeholder="Например: Баня у озера" /></Field><Field label="Контакт"><input className="input" placeholder="Telegram / WhatsApp / телефон" /></Field><Field label="Что хотите получить от сервиса?"><textarea className="input min-h-28" placeholder="Опишите формат, объект или мероприятия" /></Field></div><button onClick={() => setLeadSent(true)} className="mt-5 w-full rounded-2xl bg-stone-950 px-5 py-4 font-black text-white">{leadSent ? "Заявка сохранена в демо" : button}</button></div></div></main>;
}
