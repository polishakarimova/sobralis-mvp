/* eslint-disable @typescript-eslint/no-explicit-any */
export default function CancellationReplacementPanel({ cancelledParticipant, cancelDemoParticipant, inviteFromWaitlist, waitlistInvited, demoWaitlist }: any) {
  const firstWaitlist = demoWaitlist[0] || "первого из листа ожидания";
  return <div className="mt-5 rounded-2xl border border-rose-100 bg-rose-50 p-5"><div className="flex flex-col gap-3 md:flex-row md:items-start md:justify-between"><div><div className="text-xs font-black uppercase tracking-wide text-rose-800">Отмена и замена</div><h3 className="mt-1 text-xl font-black">Если кто-то отвалился — зовём из листа ожидания</h3><p className="mt-1 text-sm leading-6 text-stone-700">Организатор не ищет замену вручную. Сервис освобождает место и предлагает пригласить следующего человека.</p></div><div className="rounded-2xl bg-white px-4 py-3 text-sm font-black text-rose-800 ring-1 ring-rose-100">{cancelledParticipant ? `${cancelledParticipant} отменила участие` : "пока без отмен"}</div></div><div className="mt-4 grid gap-2 sm:grid-cols-2"><button onClick={() => cancelDemoParticipant("Лена")} className="rounded-2xl bg-rose-600 px-4 py-3 text-sm font-black text-white">Симулировать отмену Лены</button><button disabled={!cancelledParticipant || waitlistInvited} onClick={inviteFromWaitlist} className={`rounded-2xl px-4 py-3 text-sm font-black text-white ${cancelledParticipant && !waitlistInvited ? "bg-orange-600 hover:bg-orange-700" : "bg-stone-400"}`}>{waitlistInvited ? `${firstWaitlist} приглашена` : `Пригласить ${firstWaitlist}`}</button></div>{waitlistInvited && <div className="mt-4 rounded-2xl bg-white px-4 py-3 text-sm font-bold text-orange-800 ring-1 ring-orange-100">Участнику из листа ожидания ушло уведомление: “освободилось место, подтвердите участие”.</div>}</div>;
}
